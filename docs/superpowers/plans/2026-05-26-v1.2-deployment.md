# Budget Manager v1.2 — Deployment & QA Environment Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up a single Vercel project where `main` deploys to a production URL backed by the existing Supabase PRD (the one with real budget data), and `staging` deploys to a QA URL backed by a new, empty Supabase project — with the day-to-day dev loop still running locally against QA.

**Architecture:** No app code changes. New `vercel.json` configures the build with a test-gate. A second Supabase project is created via the MCP and seeded by applying the eight existing migrations in order. Vercel env vars differ between the Production and Preview environments so the same JS bundle talks to a different Supabase backend depending on which branch deployed it.

**Tech Stack:** Vercel (Hobby plan), Supabase (Postgres + Auth + RLS), Git branches.

---

## Reference

- **Spec:** `docs/superpowers/specs/2026-05-26-v1.2-deployment-design.md`
- **Existing PRD Supabase project:** `vouazcrsrdnaeivffwot`
- **GitHub repo:** https://github.com/Annoyingflipper/Budget-Manager

## File map

```
Budget-Manager/
├── vercel.json                          (new)
├── .env.example                         (modify — broader comments + both surfaces)
├── .env.development.local               (new locally only — gitignored)
├── .env.production.local                (new locally only — gitignored)
├── docs/superpowers/
│   └── DEPLOYMENT.md                    (new)
└── README.md                            (modify — pointer to DEPLOYMENT.md)
```

No `src/` changes. No new tests. The existing 32 tests run as the build gate.

## Pre-flight (you do once, manually)

- Sign in to Vercel via the `/plugin` Vercel integration (already done earlier in this project — confirm with `/plugin` status).
- Make sure your GitHub account that owns `Annoyingflipper/Budget-Manager` is connected to Vercel (Vercel needs read + deploy permission on the repo).
- Confirm Supabase is reachable via the plugin MCP (it has been for v1.0 and v1.1; nothing new needed).

---

## Task 1: Repo infrastructure (vercel.json + .env.example)

**Files:**
- Create: `vercel.json`
- Modify: `.env.example`

This commit lands on `main` directly because it's pure infra that needs to be in place before either Vercel deploy can run.

- [ ] **Step 1: Create `vercel.json`**

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm test && npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
```

- [ ] **Step 2: Replace `.env.example` with the new template + comments**

```
# Local development uses .env.development.local (gitignored) which should
# point at the QA Supabase project so accidental writes never touch real data.
#
# For production-build verification (`npm run preview` against PRD), use
# .env.production.local (also gitignored).
#
# In Vercel, env vars are configured in the dashboard per environment:
#   Production environment  -> PRD Supabase URL + anon key
#   Preview environment     -> QA Supabase URL + anon key

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

- [ ] **Step 3: Verify typecheck + tests + build still pass**

Run: `npm run typecheck && npm test && npm run build`
Expected: typecheck exit 0, 32 tests pass, build exit 0.

- [ ] **Step 4: Commit + push**

```bash
git add vercel.json .env.example
git commit -m "Add vercel.json with test-gated build and update .env.example for v1.2 env model"
git push origin main
```

---

## Task 2: Create the `staging` branch and push it

This is a controller / scripted task — pure git.

- [ ] **Step 1: Create the branch from current `main`**

```bash
git checkout -b staging
```

- [ ] **Step 2: Push the new branch to GitHub**

```bash
git push -u origin staging
```

Expected output: `* [new branch] staging -> staging` and `branch 'staging' set up to track 'origin/staging'.`

- [ ] **Step 3: Switch back to `main`** (so day-to-day local dev defaults to main; you'll switch to staging when you want to push QA work)

```bash
git checkout main
```

(No commit — pure git plumbing.)

---

## Task 3: Create the QA Supabase project (controller-driven via MCP)

**This task is performed by the controller.** Subagents typically don't have Supabase MCP access; the controller (the conversation runner) does. The controller:

- [ ] **Step 1: List the organization** (so we have the org ID for `create_project`)

Call `mcp__plugin_supabase_supabase__list_organizations`.

Save the `id` field of the org that already owns `vouazcrsrdnaeivffwot`. (There should be one match — same Vercel-managed org from v1.0.)

- [ ] **Step 2: Create the QA project**

Call `mcp__plugin_supabase_supabase__create_project` with:

```
name:            "Budget Manager QA"
organization_id: <org id from Step 1>
region:          "us-east-2"
plan:            "free"
```

Save the returned `id` (the new project ref). Confirm `status` is `ACTIVE_HEALTHY` (poll `get_project` if it returns a pending status — wait up to 90 seconds).

- [ ] **Step 3: Verify the new project is healthy**

Call `mcp__plugin_supabase_supabase__get_project` with the new project ref. Expected: `status: ACTIVE_HEALTHY`, `database.version: 17.x`, `region: us-east-2`.

Call `mcp__plugin_supabase_supabase__list_tables` with the new project ref and `schemas: ["public"]`. Expected: empty tables list (no schema yet).

(No commit.)

---

## Task 4: Apply the 8 migrations to QA Supabase (controller-driven via MCP)

**Controller task.** Read each migration file from the repo, then apply via `apply_migration` in order.

- [ ] **Step 1: Read migration files**

Read these files from disk:
- `supabase/migrations/0001_init.sql`
- `supabase/migrations/0002_rls.sql`
- `supabase/migrations/0003_seed_trigger.sql`
- `supabase/migrations/0004_lock_down_seed_function.sql`
- `supabase/migrations/0005_user_preferences.sql`
- `supabase/migrations/0006_extend_seed_trigger.sql`
- `supabase/migrations/0007_rls_perf.sql`
- `supabase/migrations/0008_name_length.sql`

- [ ] **Step 2: Apply each in order**

For each file, call `mcp__plugin_supabase_supabase__apply_migration` with:

```
project_id: <QA project ref from Task 3>
name:       <strip "_*.sql" suffix and the leading "000N_" prefix, then snake_case>
            e.g. "0001_init.sql" -> name "init"
                 "0006_extend_seed_trigger.sql" -> name "extend_seed_trigger"
query:      <file contents>
```

The names that should appear after all 8 calls:
1. `init`
2. `rls`
3. `seed_trigger`
4. `lock_down_seed_function`
5. `user_preferences`
6. `extend_seed_trigger`
7. `rls_perf`
8. `name_length`

Each call should return `{"success": true}`. If any fail, STOP and report — do not continue applying later migrations on a broken base.

- [ ] **Step 3: Verify schema, migration list, and advisors are clean**

Call `mcp__plugin_supabase_supabase__list_tables` with QA ref + `schemas: ["public"]` + `verbose: true`.
Expected: four tables — `income`, `categories`, `line_items`, `user_preferences` — all with `rls_enabled: true`. Row counts all 0.

Call `mcp__plugin_supabase_supabase__list_migrations` with QA ref.
Expected: eight migrations in order, names matching the list above.

Call `mcp__plugin_supabase_supabase__get_advisors` with QA ref + `type: "security"`.
Expected: a single `auth_leaked_password_protection` warning (same one PRD has, the only unfolded backlog item). NO other warnings. If anything else surfaces (especially the security-definer-exec one), STOP — that means migration 0004 didn't apply.

(No commit.)

---

## Task 5: Fetch QA credentials (controller-driven via MCP)

**Controller task.** These values are the user-safe anon credentials needed for Vercel and `.env.development.local`.

- [ ] **Step 1: Get the QA project URL**

Call `mcp__plugin_supabase_supabase__get_project_url` with the QA project ref.

Save the returned `url` — should look like `https://<qa-ref>.supabase.co`.

- [ ] **Step 2: Get the QA publishable keys**

Call `mcp__plugin_supabase_supabase__get_publishable_keys` with the QA project ref.

Save the `anon` legacy key (the JWT starting with `eyJ...`). The newer publishable key (`sb_publishable_...`) also works, but to stay consistent with the v1.0 setup we'll use the legacy anon key.

- [ ] **Step 3: Present the values to the user in chat**

Both values are safe to share (anon keys are public-by-design):

```
QA Supabase URL:      https://<qa-ref>.supabase.co
QA Supabase Anon Key: eyJ... (the JWT)
```

User saves these somewhere they can paste from in Tasks 7, 12. Tell them not to commit these values into git, but `.env.development.local` is gitignored so they're safe locally.

(No commit.)

---

## Task 6: User creates the Vercel project (user-driven, browser)

**This is a user-driven manual step.** The controller waits and confirms after.

- [ ] **Step 1: User opens the Vercel dashboard**

URL: https://vercel.com/new

- [ ] **Step 2: User imports the GitHub repo**

- Search for `Annoyingflipper/Budget-Manager` in the "Import Git Repository" section.
- Click **Import**.
- If GitHub authorization is needed, complete the OAuth flow.

- [ ] **Step 3: Configure the project**

On the configuration screen:
- **Project Name:** `budget-manager` (or `budget-manager-flipper` if `budget-manager` is taken — Vercel will tell you). Record the chosen name.
- **Framework Preset:** should auto-detect "Vite". If not, set it to Vite.
- **Root Directory:** `./` (the default).
- **Build & Output Settings:** leave defaults — `vercel.json` will override at build time.
- **Environment Variables:** SKIP for now (you'll add them in Task 7 — adding them here works too but the dashboard makes it clearer to do per environment after the project exists).

- [ ] **Step 4: Click Deploy**

The first build will run. It WILL fail because env vars aren't set yet (the bundle throws "Missing VITE_SUPABASE_URL..." at runtime — though build itself may pass since env vars are runtime, not build-time). Either way, the project is now created.

- [ ] **Step 5: User reports back**

Tell the controller:
- The Vercel project name (e.g., `budget-manager-flipper`)
- The auto-generated production URL (e.g., `https://budget-manager-flipper.vercel.app`)
- Confirmation that the project exists in the dashboard

(No commit — this is dashboard setup only.)

---

## Task 7: User configures Vercel env vars (user-driven, browser)

**This is a user-driven manual step.** Run this once for Production and once for Preview.

- [ ] **Step 1: User navigates to env var settings**

Vercel dashboard → Budget Manager project → **Settings** → **Environment Variables**.

- [ ] **Step 2: Add `VITE_SUPABASE_URL` for Production**

- Click **Add New**.
- Name: `VITE_SUPABASE_URL`
- Value: `https://vouazcrsrdnaeivffwot.supabase.co` (the PRD URL)
- Environments: check **Production** only. Uncheck Preview and Development.
- Click **Save**.

- [ ] **Step 3: Add `VITE_SUPABASE_ANON_KEY` for Production**

- Click **Add New**.
- Name: `VITE_SUPABASE_ANON_KEY`
- Value: The PRD anon key (the long JWT). Get it from the .env.local file in the repo, or from the Supabase dashboard for project `vouazcrsrdnaeivffwot` → Settings → API.
- Environments: check **Production** only.
- Click **Save**.

- [ ] **Step 4: Add `VITE_SUPABASE_URL` for Preview**

- Click **Add New**.
- Name: `VITE_SUPABASE_URL`
- Value: The QA URL from Task 5 (`https://<qa-ref>.supabase.co`).
- Environments: check **Preview** only.
- Click **Save**.

- [ ] **Step 5: Add `VITE_SUPABASE_ANON_KEY` for Preview**

- Click **Add New**.
- Name: `VITE_SUPABASE_ANON_KEY`
- Value: The QA anon key from Task 5.
- Environments: check **Preview** only.
- Click **Save**.

- [ ] **Step 6: Verify the env var list**

The Environment Variables page should now show 4 entries:
- `VITE_SUPABASE_URL` (Production) — PRD URL
- `VITE_SUPABASE_ANON_KEY` (Production) — PRD anon
- `VITE_SUPABASE_URL` (Preview) — QA URL
- `VITE_SUPABASE_ANON_KEY` (Preview) — QA anon

User reports back: "env vars set."

(No commit.)

---

## Task 8: Trigger a fresh production deploy with env vars

The first deploy from Task 6 ran without env vars. Now that they're set, force a redeploy of `main`.

- [ ] **Step 1: Trigger a redeploy from the Vercel dashboard**

Vercel dashboard → Budget Manager → Deployments → find the most recent `main` deployment → click the three-dot menu → **Redeploy** → confirm.

This re-runs `vercel.json`'s `npm test && npm run build` with the new env vars baked into the bundle.

- [ ] **Step 2: Wait for the deploy to finish**

Watch the deployment status. Expected: **Ready** (green) within 1-2 minutes.

If it fails on tests, that's a real test regression — investigate locally and fix on `main` before continuing.

- [ ] **Step 3: Visit the production URL in your browser**

Open the production URL (from Task 6 Step 5).

Expected:
- Login screen renders in Peach light theme.
- Click **Log in**, enter your existing email + password.
- Complete MFA challenge.
- Dashboard loads showing your real June data (categories, line items, balances).

If the login screen renders but log-in fails with a generic error, env vars are likely wrong in Vercel — re-check Task 7.

User reports back: "PRD deploy verified."

(No commit.)

---

## Task 9: Push `staging` and verify the QA deploy

- [ ] **Step 1: Make sure `staging` is up-to-date with `main`**

```bash
git checkout staging
git pull origin staging
git merge main --no-edit
git push origin staging
```

Vercel detects the push and starts a preview deployment for the `staging` branch.

- [ ] **Step 2: Find the staging preview URL**

In the Vercel dashboard → Deployments tab → look for the most recent deploy with branch `staging`. The URL looks like `budget-manager-git-staging-<username>.vercel.app`.

- [ ] **Step 3: Wait for the deploy to finish**

Expected: **Ready** within 1-2 minutes.

- [ ] **Step 4: Open the staging URL in your browser**

Expected:
- Login screen renders.
- This is the QA Supabase, so you do NOT have an account yet. Don't try to log in as your PRD user.

User reports back: "QA URL is up at <staging-preview-URL>."

(No commit.)

---

## Task 10: Add the `budget-manager-qa.vercel.app` domain alias

**User-driven, browser.**

- [ ] **Step 1: Vercel dashboard → Settings → Domains**

Click **Add Domain**.

- [ ] **Step 2: Enter the QA hostname**

Type `budget-manager-qa.vercel.app`.

If it's taken, suffix with a personal identifier (e.g., `budget-manager-qa-flipper.vercel.app`). Record the chosen value.

Click **Add**.

- [ ] **Step 3: Assign the domain to the `staging` branch**

On the domain row that appeared, set **Git Branch** to `staging`. (Some Vercel UIs show this as "Edit" on the domain row.)

- [ ] **Step 4: Verify**

Open `https://budget-manager-qa.vercel.app` (or the suffixed variant) in your browser.

Expected: same login screen as Task 9 Step 4 — the alias serves the staging preview deployment.

User reports back: "QA alias live at <qa-URL>."

(No commit.)

---

## Task 11: Sign up on QA (user-driven, browser)

- [ ] **Step 1: Open the QA URL**

`https://budget-manager-qa.vercel.app` (or variant).

- [ ] **Step 2: Sign up**

Click "Need an account? Sign up". Enter a real email you can check (can be the same email as your PRD account — Supabase auth is scoped to each project, so this creates a separate QA-only user). Choose a password (≥6 chars).

- [ ] **Step 3: Verify email**

Check your inbox, click the verification link, return to the QA URL, log in.

- [ ] **Step 4: Enroll an MFA factor**

The MFAEnroll screen appears with a QR code. Scan it with an authenticator app (use a separate entry from your PRD account — e.g., name it "Budget QA" in the authenticator). Enter the 6-digit code.

- [ ] **Step 5: Verify the dashboard loads with seeded categories**

Expected:
- Income = $0 / $0
- 8 categories, all rendered as `EmptyCategoryCard` ("Nothing here yet" + "+ Add first X" buttons)
- Grand Totals at the bottom showing $0 / $0 / $0
- Theme defaults to Peach light (since `user_preferences` was seeded by the trigger)

If any of the above is missing, the migrations didn't apply cleanly — go back to Task 4 step 3 and re-verify.

User reports back: "QA sign-up + dashboard verified."

(No commit.)

---

## Task 12: Set up local `.env.development.local` and verify local dev hits QA

**User does the file creation** (controller knows the values from Task 5 but the file is gitignored and the user owns their machine).

- [ ] **Step 1: Create `.env.development.local`**

In the repo root (`/Users/vmaniglia/Documents/GitHub/Budget-Manager`):

```bash
cat > .env.development.local <<'EOF'
VITE_SUPABASE_URL=<QA URL from Task 5>
VITE_SUPABASE_ANON_KEY=<QA anon key from Task 5>
EOF
```

Replace the placeholders with the actual values the controller surfaced in Task 5.

- [ ] **Step 2 (optional): Create `.env.production.local`**

This is only needed if you ever want to run `npm run preview` locally against PRD data. Skip if you don't plan to.

```bash
cat > .env.production.local <<'EOF'
VITE_SUPABASE_URL=https://vouazcrsrdnaeivffwot.supabase.co
VITE_SUPABASE_ANON_KEY=<paste from existing .env.local>
EOF
```

- [ ] **Step 3: Confirm both files are gitignored**

```bash
git status --short
git check-ignore -v .env.development.local .env.production.local
```

Expected: `git status` shows them as ignored (no entries for them). `git check-ignore` confirms `.gitignore` line 7 (`.env*.local`) matches.

- [ ] **Step 4: Decide what to do with the old `.env.local`**

You currently have a `.env.local` at the repo root with PRD values (from v1.0). With `.env.development.local` now overriding it for `npm run dev`, the old `.env.local` is no longer needed for daily dev. Two options:

(a) Delete it (`rm .env.local`) since local dev should hit QA now.
(b) Keep it as a backup for any code that doesn't use Vite mode-aware loading.

Recommended: **delete it** so there's only one source of truth per surface.

```bash
rm .env.local
```

- [ ] **Step 5: Start the dev server and verify**

```bash
npm run dev
```

Open `http://localhost:5173`. You should see the login screen but NOT be able to log in with your PRD credentials (since this is the QA Supabase now). Use the QA account you signed up with in Task 11 — log in successfully → empty dashboard.

Add a single test line item (e.g., "Test Local" in Services with projected=$10). Tab away.

Now refresh `https://budget-manager-qa.vercel.app` in another tab. The "Test Local" item should appear there.

Stop the dev server (Ctrl-C).

User reports back: "Local dev verified hitting QA — added test item appeared on QA URL too."

(No commit — env files are gitignored.)

---

## Task 13: Write `docs/superpowers/DEPLOYMENT.md`

**Files:**
- Create: `docs/superpowers/DEPLOYMENT.md`

This commit lands on `main` so it's available in production browsing.

- [ ] **Step 1: Create `docs/superpowers/DEPLOYMENT.md`**

```markdown
# Budget Manager — Deployment & QA Workflow

This document is the operational reference for shipping changes through QA → PRD safely. The architectural decisions live in `docs/superpowers/specs/2026-05-26-v1.2-deployment-design.md`.

## Architecture at a glance

```
GitHub Annoyingflipper/Budget-Manager
                │
                ▼
       Single Vercel project
                │
   ┌────────────┴────────────┐
   main branch          staging branch
   Production env       Preview env
   PRD Supabase         QA Supabase
   |                    |
   budget-manager       budget-manager-qa
     .vercel.app          .vercel.app
                                ▲
                                │
                  Local dev (npm run dev) also hits QA
```

- **Production:** `main` → `https://budget-manager.vercel.app` → PRD Supabase (`vouazcrsrdnaeivffwot`)
- **QA:** `staging` → `https://budget-manager-qa.vercel.app` → QA Supabase
- **Local dev:** `.env.development.local` → QA Supabase

## Day-to-day workflow

1. Code locally on a feature branch (`git checkout -b feat/something staging`) or directly on `staging`.
2. Run tests + dev server locally. `npm test` and `npm run dev`. Local talks to QA Supabase — safe to break things.
3. If you wrote a new SQL migration, apply it to QA via the Supabase MCP `apply_migration` tool (project_id = QA ref).
4. Commit, push to `staging`. Vercel builds + deploys to `budget-manager-qa.vercel.app` automatically.
5. Smoke-test the change on the QA URL.
6. **Once QA is green**, apply the same SQL migration to PRD via `apply_migration` (project_id = `vouazcrsrdnaeivffwot`).
7. Merge `staging` → `main`. Vercel builds + deploys to production.
8. Quick smoke check on the PRD URL.

## Env var reference

| Surface | File / Dashboard | Points at |
|---|---|---|
| `npm run dev` | `.env.development.local` (gitignored) | QA Supabase |
| `npm run preview` (rare) | `.env.production.local` (gitignored) | PRD Supabase |
| Vercel Production | Dashboard → Env Vars → Production | PRD Supabase |
| Vercel Preview (all branches) | Dashboard → Env Vars → Preview | QA Supabase |

## Applying a new migration

1. Write the SQL: `supabase/migrations/000N_<name>.sql`.
2. Commit on `staging`.
3. Apply to QA via MCP:
   - Tool: `mcp__plugin_supabase_supabase__apply_migration`
   - `project_id`: QA Supabase ref
   - `name`: the migration name in snake_case (no leading number, no `.sql`)
   - `query`: the contents of the SQL file
4. Verify QA: `list_tables`, `list_migrations`, `get_advisors` — all clean.
5. Push to `staging`. Vercel deploys QA.
6. Smoke-test on QA URL.
7. Apply the **same** migration to PRD (same MCP call, project_id = `vouazcrsrdnaeivffwot`).
8. Merge `staging` → `main`. Vercel deploys PRD.

## Rollback

**Production deploy broke something.**
Vercel dashboard → Deployments → find the last known-good deployment → three-dot menu → **Promote to Production**. Instant rollback to that bundle. Then fix the broken commit on `staging`.

**Migration broke PRD.**
Two options, in priority order:
1. **Forward-fix migration** — write a new migration on QA that fixes or reverts the broken one, validate it on QA, then apply to PRD. Safer; preserves all data.
2. **Supabase Point-in-Time Recovery** — Supabase dashboard → Database → PITR → choose a timestamp before the bad migration. Destroys all writes since that timestamp. Only use if data is genuinely corrupted and option 1 isn't viable.

**QA and PRD schemas drifted.**
Run `list_migrations` against both project refs and diff. Apply the missing migration to the lagging side.

## Common failure modes

- **Vercel build fails on push:** check the Vercel build log. Usually tests, sometimes env var typos.
- **App loads but Supabase calls 401/permission-denied:** env vars are pointing at the wrong project, or the user hasn't completed MFA (AAL2 required for budget reads).
- **App loads but no data on PRD URL:** wrong Vercel env (probably Preview env vars are set on Production by mistake). Double-check the Production column in Vercel → Env Vars.

## How to add a new version sub-page in Notion

When you ship a new version (v1.3, v1.4, etc.):
1. Open the Budget Manager hub in Notion.
2. Duplicate the most recent version sub-page.
3. Rename to the new version. Update the **What shipped** and **Smoke test** sections.
4. Link the new sub-page from the hub's Versions index.
5. Mark the previous version Complete once the new one is validated.
```

- [ ] **Step 2: Verify, commit, and push**

```bash
git checkout main
git add docs/superpowers/DEPLOYMENT.md
git commit -m "Add DEPLOYMENT.md with workflow, migration runbook, and rollback notes"
git push origin main
```

---

## Task 14: Update `README.md` with v1.2 pointers

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace `README.md`**

```markdown
# Budget Manager

A personal budgeting web app that tracks projected vs. actual values across eight
expense categories, with email/password + TOTP MFA auth. Themeable (Peach default,
Sage, Lavender) with light + dark modes. Built on React + Vite + Tailwind + Supabase
and deployed to Vercel.

## Environments

- **Production:** [budget-manager.vercel.app](https://budget-manager.vercel.app) — backed by the PRD Supabase project. Deployed automatically from the `main` branch.
- **QA:** [budget-manager-qa.vercel.app](https://budget-manager-qa.vercel.app) — backed by a separate QA Supabase project. Deployed automatically from the `staging` branch.
- **Local dev** (`npm run dev`): also hits the QA Supabase project, so day-to-day coding never touches production data.

See [`docs/superpowers/DEPLOYMENT.md`](docs/superpowers/DEPLOYMENT.md) for the full workflow, migration runbook, and rollback procedures.

## Local development

1. Clone the repo and `npm install`.
2. Get the QA Supabase URL + anon key (from the project owner; or, if you're the owner, from `mcp__plugin_supabase_supabase__get_project_url` and `get_publishable_keys`).
3. Create `.env.development.local` at the repo root:
   ```
   VITE_SUPABASE_URL=https://<qa-ref>.supabase.co
   VITE_SUPABASE_ANON_KEY=<qa-anon-key>
   ```
4. `npm run dev` — http://localhost:5173.

## Scripts

```
npm run dev          # dev server at http://localhost:5173, points at QA via .env.development.local
npm run build        # production build (runs `npm test` first via vercel.json buildCommand)
npm run preview      # preview the production build (use .env.production.local if testing against PRD)
npm test             # vitest run, 32 tests across money utilities, ThemeProvider, LineItemRow, DraftRow
npm run typecheck    # TypeScript-only check
```

## Architecture

- **Frontend:** React 18 + Vite 5 + TypeScript (strict) + Tailwind CSS 3 with CSS-variable theme tokens.
- **Backend:** Supabase (Postgres + Auth + RLS + Storage). No custom server.
- **Auth:** email + password + TOTP MFA. Budget data is unreachable until the session reaches AAL2 (MFA completed), enforced by Row-Level Security policies.
- **Data model:** four tables — `income`, `categories`, `line_items`, `user_preferences`. All derived math (difference, subtotals, totals, balance) is computed in the UI.
- **Editing:** every cell is a live input. Changes auto-save on blur with optimistic UI; failures revert the local state.
- **Theming:** three preset themes (Peach, Sage, Lavender) × light/dark, persisted per user in `user_preferences`. Single CSS variable system; switching themes is one HTML attribute change.
- **Deployment:** single Vercel project, `main` → production, `staging` → preview (aliased to the QA URL).

## Project history

- `docs/superpowers/specs/` — design specs for each version
- `docs/superpowers/plans/` — implementation plans for each version
- `docs/superpowers/DEPLOYMENT.md` — operational deployment workflow

## Deferred (future versions)

- **v1.3:** WebAuthn / passkey MFA + leaked-password protection + remaining test gaps
- **v1.4+:** Month rollover and history, custom user-editable categories, sharing budgets with another user, charts + CSV export — each its own brainstorm + spec + plan cycle.
```

- [ ] **Step 2: Commit + push**

```bash
git add README.md
git commit -m "Update README with v1.2 environments, dev workflow, and DEPLOYMENT.md pointer"
git push origin main
```

---

## Task 15: Update Notion — create v1.2 sub-page and mark version index

**Controller task, via the plugin Notion MCP.**

- [ ] **Step 1: Create the v1.2 smoke-test sub-page under the Budget Manager hub**

Hub page ID: `36bb6bed-f453-81cb-8f5a-c62e801b5cfd`.

Call `mcp__plugin_Notion_notion__notion-create-pages` with `parent.page_id` = hub ID, and a single page with `title: "v1.2 Smoke Tests"`, `icon: "🚀"`, and the content below:

```markdown
**Status:** Awaiting smoke test (run after Task 12 of the v1.2 plan completes)
**Final v1.2 commits:** see `docs/superpowers/plans/2026-05-26-v1.2-deployment.md`

## What shipped in v1.2

- New `vercel.json` with `npm test && npm run build` gating every deploy
- New `staging` branch tracking `origin/staging` — Vercel preview environment
- New Supabase project "Budget Manager QA" (us-east-2), schema-identical to PRD via the 8 existing migrations
- Single Vercel project linked to the GitHub repo:
  - `main` → production at https://budget-manager.vercel.app → PRD Supabase
  - `staging` → preview at https://budget-manager-qa.vercel.app → QA Supabase (via domain alias)
- Vercel env vars configured per environment (Production = PRD, Preview = QA)
- Local dev now points at QA Supabase via `.env.development.local`
- New `docs/superpowers/DEPLOYMENT.md` documenting workflow, migrations, rollback
- README updated with the two environments and the new dev flow

No source code changes. No new tests. The 32 existing tests remain the build gate.

## Smoke test checklist

### Production (`https://budget-manager.vercel.app`)

- [ ] Production URL loads the Peach-light login screen
- [ ] Logging in with the existing PRD account works
- [ ] MFA challenge appears (returning user) and succeeds
- [ ] Dashboard shows real June expense data
- [ ] All v1.1 features still work (theme switcher, dark mode, mobile layout, +Add item flow)
- [ ] No console errors in DevTools

### QA (`https://budget-manager-qa.vercel.app`)

- [ ] QA URL loads the Peach-light login screen
- [ ] PRD account does NOT work here (different Supabase project)
- [ ] New QA account sign-up sends a verification email
- [ ] MFA enrollment flow works (separate authenticator entry from the PRD one)
- [ ] Empty dashboard renders with 8 EmptyCategoryCards
- [ ] Adding a test line item persists
- [ ] All v1.1 features still work

### Local dev (`http://localhost:5173`)

- [ ] `npm run dev` reads `.env.development.local` and points at QA
- [ ] PRD account does NOT log in locally
- [ ] QA account logs in successfully
- [ ] A line item added locally appears on the QA URL after refresh
- [ ] Tests still pass: `npm test` → 32/32 green

### Migration round-trip (optional but recommended)

- [ ] Pick a trivial no-op migration (e.g. `comment on table public.income is 'budget income singleton'`) to validate the QA → PRD flow:
  - [ ] Apply to QA via `apply_migration`
  - [ ] Verify on QA `list_migrations`
  - [ ] Apply the same SQL to PRD via `apply_migration`
  - [ ] Verify on PRD `list_migrations`

### Rollback drill (optional)

- [ ] In Vercel dashboard, find a previous Production deploy → "Promote to Production" → confirm the prod URL reverts → "Promote to Production" on the latest to restore. (No actual breakage; just exercise the muscle.)

## Sign-off

When all required boxes above are checked and no regression found, mark this page Complete and update the v1.2 entry on the parent Budget Manager hub.
```

- [ ] **Step 2: Update the Budget Manager hub Versions section**

Call `mcp__plugin_Notion_notion__notion-update-page` with:
- `page_id`: hub ID
- `command`: `update_content`
- `content_updates`:
  ```
  [{
    "old_str": "## Roadmap (deferred from earlier versions)\n\n- **v1.2:** Vercel deployment + QA Supabase project + Vercel preview environments",
    "new_str": "**v1.2** — Vercel PRD + QA deployment + new QA Supabase project. 🚧 Awaiting smoke test\n\n---\n\n## Roadmap (deferred from earlier versions)"
  }]
  ```

  This swaps the v1.2 entry from "future roadmap" into the "Versions" index list. Also embed the v1.2 sub-page URL into the Versions section via a follow-up `insert_content` if the embed didn't auto-populate.

- [ ] **Step 3: Verify in the Notion app**

User opens the Budget Manager hub in Notion. Confirms:
- The v1.2 sub-page appears in the Versions section.
- The v1.2 line in the Versions index says "🚧 Awaiting smoke test".

(No git commit — Notion only.)

---

## Task 16: User runs the v1.2 smoke test

**User-driven via the Notion v1.2 sub-page checklist.**

- [ ] **Step 1: User walks through every required checkbox on the v1.2 Notion sub-page**

PRD section, QA section, Local section. Optional sections (migration round-trip, rollback drill) at user's discretion.

- [ ] **Step 2: Resolve any failures**

If anything fails:
- Re-check the corresponding task in this plan
- For env-var issues: Task 7
- For migration issues: Task 4
- For Vercel build failures: check Vercel logs, fix locally, push again

- [ ] **Step 3: User reports completion**

User tells the controller: "v1.2 smoke test passed."

(No commit.)

---

## Task 17: Mark v1.2 complete and close out

**Controller task once smoke test passes.**

- [ ] **Step 1: Update the v1.2 Notion sub-page**

Call `notion-update-page` with `command: update_properties`, set the icon from 🚀 to ✅, leave the title as-is.

Then call `notion-update-page` with `command: update_content`, swap the **Status** line at the top from "Awaiting smoke test" to "Complete · Validated <today's date>".

Also check the boxes on the smoke test checklist as a record (use `update_content` with `content_updates` to replace `- [ ]` with `- [x]` for the required items the user confirmed).

- [ ] **Step 2: Update the Budget Manager hub Versions index**

Swap the v1.2 line from "🚧 Awaiting smoke test" to "✅ Complete (validated <today's date>)".

- [ ] **Step 3: Sync `staging` with `main` (DEPLOYMENT.md + README only landed on main)**

```bash
git checkout staging
git pull origin staging
git merge main --no-edit
git push origin staging
git checkout main
```

This re-runs the QA build with the merged docs — no behavior change, but keeps the branches in lockstep for future v1.3 work. Expected: Vercel queues a new staging build; once green, both URLs serve the same code.

- [ ] **Step 4: Final git status check**

```bash
git status
git log --oneline origin/main..HEAD 2>&1 || echo '(no unpushed commits)'
git log --oneline origin/staging..staging 2>&1 || echo '(staging up to date)'
```

Expected: working tree clean. All v1.2 commits are pushed. `staging` matches `main`.

- [ ] **Step 5: Report to user**

Summary message including: production URL, QA URL, commit count, what's next (v1.3 = passkey + test gaps).

---

## Done

v1.2 is complete. The app now has two real environments with a proper QA → PRD flow. The next likely items (each its own brainstorm cycle):

- **v1.3:** WebAuthn passkey MFA + remaining test gaps + leaked-password protection toggle.
- **v1.4+:** Month rollover + history, custom user-editable categories, sharing budgets with another user, charts + CSV export.

Each of these will follow the same brainstorm → spec → plan → execute → smoke-test pattern documented in `DEPLOYMENT.md`.
